#include "main.h"
#include "linked_list.h"

int main()
{
	linked_list<int> list;

	//student_stack stack;
	//stack.push(student(1, "Alex", "Vance", "A"));
	//stack.push(student(2, "Gordon", "Freeman", "A"));
	//stack.push(student(3, "Eli", "Vance", "B"));
	//stack.pop();

	//stack.push(student(4, "Isaac", "Kleiner", "B"));
	//stack.print();

	return 0;
}
